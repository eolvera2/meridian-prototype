/**
 * Task1Wrapper Component
 * Narrow view wrapper for Task 1 - renders via iframe to /task1-home.
 */

import { TaskWrapper } from "../shared/TaskWrapper";

export const Task1Wrapper = () => <TaskWrapper taskNumber={1} />;

export default Task1Wrapper;
