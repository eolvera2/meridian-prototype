/**
 * Task4StartWrapper Component
 * Narrow view wrapper for Task 4 - renders via iframe to /task4-home.
 * Task 4: Start in Ambient mode (Mic Off), success when user adds a Referral letter.
 */

import { TaskWrapper } from "../shared/TaskWrapper";

export const Task4StartWrapper = () => <TaskWrapper taskNumber={4} />;

export default Task4StartWrapper;
