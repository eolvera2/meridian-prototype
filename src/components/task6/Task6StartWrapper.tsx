/**
 * Task6StartWrapper Component
 * Narrow view wrapper for Task 6 - renders via iframe to /task6-home.
 * Task 6: Start with Orders expanded, success when user reaches Settings -> Documents.
 */

import { TaskWrapper } from "../shared/TaskWrapper";

export const Task6StartWrapper = () => <TaskWrapper taskNumber={6} />;

export default Task6StartWrapper;
